# BreedingMethod
"""
entities/generated_breeding_method.py.py — BreedingMethod Pydantic model + BreedingMethodQuery.

This file is generated by the Thymeleaf Python template (EntityClass.txt).
Keep hand-written infrastructure dependencies minimal:
  - Pydantic BaseModel for the entity
  - BaseQuery from brapi._query

Generated by:
  BrAPI Schema Tools 0.57.0
"""
from __future__ import annotations

from typing import TYPE_CHECKING, Any, Dict, Optional, Union
from datetime import date, datetime
from enum import Enum, IntEnum
from brapi._compat import StrEnum

import pandas as pd
from pydantic import BaseModel, ConfigDict
if TYPE_CHECKING:
    from brapi.entities.generated_pedigree_node import PedigreeNode
    from brapi.entities.generated_germplasm import Germplasm
    


from .._query import BaseQuery
from .._http import HttpTransport
from .._result import BrapiResult


# ---------------------------------------------------------------------------
# Primary model
# ---------------------------------------------------------------------------

class BreedingMethod(BaseModel):
    """
    The techniques and protocol used to produce a Cross or Germplasm

    Required fields: ``abbreviation``,
    ``breedingMethodDbId``,
    ``breedingMethodName``,
    ``description``

    All other fields are optional.  Nested relationship lists (``) are parsed into sub-models when present; unknown extra fields are
    accepted (``extra="allow"``) to survive schema evolution without breaking.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---
    abbreviation: str
    breedingMethodDbId: str
    breedingMethodName: str
    description: str


# ---------------------------------------------------------------------------
# Custom DataFrame flattener
# ---------------------------------------------------------------------------

def breeding_method_to_df(items: list[BreedingMethod]) -> pd.DataFrame:
    """
    Convert a list of ``BreedingMethod`` objects to a flat ``pd.DataFrame``.

    Nested sub-model lists (``) are serialised to JSON strings so each BreedingMethod remains
    one row.  Relationship objects (``) are expanded one level: any ``*DbId`` and ``*Name`` fields are hoisted
    to top-level columns.
    """
    rows: list[dict[str, Any]] = []
    for item in items:
        row = item.model_dump(mode="python", exclude_none=True)

        # Flatten one-to-one relationships: extract DbId/Name into top-level cols
        for rel in (

        ):
            obj = row.pop(rel, None)
            if isinstance(obj, dict):
                for k, v in obj.items():
                    if k.endswith(("DbId", "Name", "PUI")):
                        row[f"{rel}_{k}"] = v

        # Serialise one-to-many lists to strings (one row per BreedingMethod)
        for arr_field in (

        ):
            arr = row.pop(arr_field, None)
            if arr:
                import json
                row[arr_field] = json.dumps(arr, default=str)

        rows.append(row)

    return pd.DataFrame(rows)

# ---------------------------------------------------------------------------
# Query builder
# ---------------------------------------------------------------------------

_CRUD_ENDPOINT = "breedingmethods"

class BreedingMethodQuery(BaseQuery[BreedingMethod]):
    """
    Fluent query builder for the BrAPI BreedingMethod resource.

    Create via ``BrapiClient.breeding_method`` — do not instantiate directly.
    
    Materialise with::

        result = q1.fetch()          # BrapiResultBreedingMethod] (lazy)
        df     = q1.fetch().to_df()  # triggers HTTP, returns DataFrame
        items  = list(q1.fetch())    # same
    """

    def __init__(self, http: HttpTransport) -> None:
        super().__init__(
            http=http,
            endpoint=_CRUD_ENDPOINT,
            model_cls=BreedingMethod,
            http_method="GET",
            to_df_fn=breeding_method_to_df,
        )


    def list(
        self,
        *,
        page_size: Optional[int] = None,
    ) -> BrapiResult[BreedingMethod]:
        """
        Execute the query by calling ``GET breedingmethods``.

        Uses the same filter state built with the fluent methods.  Parameters
        that accept multiple values are passed as repeated query-string keys so
        the server can filter on all supplied values.

        .. note::
            Some filter parameters supported by ``search()``
            (``synonyms``, ``familyCodes``, ``binomialNames``, ``collections``)
            are **not** available on the list endpoint and will be silently
            ignored.

        
        Args:
            page_size: Override the page size for this call only.

        Returns:
            Lazy ``BrapiResult[BreedingMethod]``.
        """
        _page_size = page_size if page_size is not None else self._page_size
        max_pages = self._max_pages

        # Translate search params (plural body keys) → GET query params (singular)
        get_params: Dict[str, Any] = {}

        http = self._http
        model_cls = self._model_cls
        to_df_fn = self._to_df_fn

        def _fetcher() -> List[BreedingMethod]:
            records = http.fetch_all_pages(
                endpoint=_CRUD_ENDPOINT,
                method="GET",
                params=get_params,
                page_size=_page_size,
                max_pages=max_pages,
            )
            return [model_cls(**r) for r in records]  # type: ignore[call-arg]

        return BrapiResult(fetcher=_fetcher, to_df_fn=to_df_fn)  # type: ignore[arg-type]

    # ------------------------------------------------------------------
    # CRUD operations — execute immediately (not lazy BrapiResult)
    # ------------------------------------------------------------------
    def get_by_id(self, breeding_method_dbid: str) -> "BreedingMethod":
        """
        Retrieve a single BreedingMethod by its database ID.

        Calls ``GET breedingmethods/{breedingMethodDbId}``.

        Args:
            breeding_method_dbid: The ``breedingMethodDbId`` to retrieve.

        Returns:
            A single ``BreedingMethod`` object.
        """
        record = self._http.get_one(f"{_CRUD_ENDPOINT}/{breeding_method_dbid}")
        return BreedingMethod(**record)





# Generated by Schema Tools Generator Version: '0.57.0'