# Method
"""
entities/generated_method.py.py — Method Pydantic model + MethodQuery.

This file is generated by the Thymeleaf Python template (EntityClass.txt).
Keep hand-written infrastructure dependencies minimal:
  - Pydantic BaseModel for the entity
  - BaseQuery from brapi._query

Generated by:
  BrAPI Schema Tools 0.57.0 (
"""
from __future__ import annotations

from typing import Any, Dict, List, Optional, Union
from datetime import date, datetime

import pandas as pd
from pydantic import BaseModel, ConfigDict


from brapi.generated_common import OntologyReference, ExternalReference, AdditionalInfo

from .._query import BaseQuery
from .._http import HttpTransport
from .._result import BrapiResult


# ---------------------------------------------------------------------------
# Primary model
# ---------------------------------------------------------------------------

class Method(BaseModel):
    """
    A description of the way an Observation should be collected. 
&lt;br&gt;For example, an ObservationVariable might be defined with a Trait of &quot;plant height&quot;, a Scale of &quot;meters&quot;, and a Method of &quot;tape measure&quot;. This variable would be distinct from a variable with the Method &quot;estimation&quot; or &quot;drone image processing&quot;. 

    Required fields: ``methodDbId``,
    ``methodName``

    All other fields are optional.  Nested relationship lists (``externalReferences``) are parsed into sub-models when present; unknown extra fields are
    accepted (``extra="allow"``) to survive schema evolution without breaking.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---
    methodDbId: str
    methodName: str
    # --- Scalar optional ---
    bibliographicalReference: Optional[str] = None
    description: Optional[str] = None
    formula: Optional[str] = None
    methodClass: Optional[str] = None
    methodPUI: Optional[str] = None

    # --- Nested lists ---
    externalReferences: Optional[List[ExternalReference]] = None
    # --- Relationship IDs (many-to-one; full objects are dropped in list endpoints) ---
    # These arrive as simple dicts when present; use Any to avoid failures.

    additionalInfo: Optional[Any] = None
    ontologyReference: Optional[Any] = None


# ---------------------------------------------------------------------------
# Custom DataFrame flattener
# ---------------------------------------------------------------------------

def method_to_df(items: List[Method]) -> pd.DataFrame:
    """
    Convert a list of ``Method`` objects to a flat ``pd.DataFrame``.

    Nested sub-model lists (``) are serialised to JSON strings so each Method remains
    one row.  Relationship objects (``) are expanded one level: any ``*DbId`` and ``*Name`` fields are hoisted
    to top-level columns.
    """
    rows: List[Dict[str, Any]] = []
    for item in items:
        row = item.model_dump(mode="python", exclude_none=True)

        # Flatten one-to-one relationships: extract DbId/Name into top-level cols
        for rel in (

        ):
            obj = row.pop(rel, None)
            if isinstance(obj, dict):
                for k, v in obj.items():
                    if k.endswith(("DbId", "Name", "PUI")):
                        row[f"{rel}_{k}"] = v

        # Serialise one-to-many lists to strings (one row per Method)
        for arr_field in (

        ):
            arr = row.pop(arr_field, None)
            if arr:
                import json
                row[arr_field] = json.dumps(arr, default=str)

        rows.append(row)

    return pd.DataFrame(rows)

# ---------------------------------------------------------------------------
# Query builder
# ---------------------------------------------------------------------------

_CRUD_ENDPOINT = "/methods"

# Maps POST  body parameter names → GET /methods query parameter
# names.  Parameters absent from this dict (or mapped to None) are not forwarded
# to the list endpoint because GET /methods does not support them.
_LIST_PARAM_MAP: Dict[str, Optional[str]] = {
    "commonCropNames": "commonCropName", 
    "ontologyDbIds": "ontologyDbId", 
    "programDbIds": "programDbId", 
    "programNames": "programName", 
    "methodDbIds": "methodDbId", 
    "observationVariableDbIds": "observationVariableDbId"
    
}
class MethodQuery(BaseQuery[Method]):
    """
    Fluent query builder for the BrAPI Method resource.

    Create via ``BrapiClient.method`` — do not instantiate directly.
    
    All filter methods return a new ``MethodQuery`` (immutable builder
    pattern) so the same base query can be forked::

        base = client.method.common_crop_names("Tomatillo")
        q1   = base.program_dbids("8f5de35b")
        q2   = base.program_names("Better Breeding Program")
    You can provide single values one at time or a list to each filter method, for example::
        q3 = client.method.common_crop_names("Tomatillo").common_crop_names("Paw Paw")  # one value as time
        q4 = client.method.common_crop_names(["Tomatillo", "Paw Paw", ...])  # list
        
    Materialise with::

        result = q1.fetch()          # BrapiResultMethod] (lazy)
        df     = q1.fetch().to_df()  # triggers HTTP, returns DataFrame
        items  = list(q1.fetch())    # same
    """

    def __init__(self, http: HttpTransport) -> None:
        super().__init__(
            http=http,
            endpoint=_CRUD_ENDPOINT,
            model_cls=Method,
            http_method="POST",
            to_df_fn=method_to_df,
        )

    # ------------------------------------------------------------------
    # Filter methods — map directly to MethodRequest parameters
    # ------------------------------------------------------------------


    # --- commonCropNames ---

    def common_crop_names(self, common_crop_names: Union[str, List[str]]) -> "MethodQuery":
        """The BrAPI Common Crop Name is the simple, generalized, widely accepted name of the organism being researched. It is most often used in multi-crop systems where digital resources need to be divided at a high level. Things like &#39;Maize&#39;, &#39;Wheat&#39;, and &#39;Rice&#39; are examples of common crop names.

Use this parameter to only return results associated with the given crops. 

Use `GET /commoncropnames` to find the list of available crops on a server.

        Example::
            client.method
                .common_crop_names("Tomatillo")
                .fetch()
                .to_df()
            
            client.method
                .common_crop_names(["Tomatillo", "Paw Paw"])
                .fetch()
                .to_df()
            
        """
        return self._set_param("commonCropNames", common_crop_names)  # type: ignore[return-value]

    # --- ontologyDbIds ---

    def ontology_dbids(self, ontology_dbids: Union[str, List[str]]) -> "MethodQuery":
        """The unique identifier for an ontology definition. Use this parameter to filter results based on a specific ontology 

  Use `GET /ontologies` to find the list of available ontologies on a server.

        
        """
        return self._set_param("ontologyDbIds", ontology_dbids)  # type: ignore[return-value]

    # --- programDbIds ---

    def program_dbids(self, program_dbids: Union[str, List[str]]) -> "MethodQuery":
        """A BrAPI Program represents the high level organization or group who is responsible for conducting trials and studies. Things like Breeding Programs and Funded Projects are considered BrAPI Programs. 

Use this parameter to only return results associated with the given programs. 

Use `GET /programs` to find the list of available programs on a server.

        Example::
            client.method
                .program_dbids("8f5de35b")
                .fetch()
                .to_df()
            
            client.method
                .program_dbids(["8f5de35b", "0e2d4a13"])
                .fetch()
                .to_df()
            
        """
        return self._set_param("programDbIds", program_dbids)  # type: ignore[return-value]

    # --- programNames ---

    def program_names(self, program_names: Union[str, List[str]]) -> "MethodQuery":
        """Use this parameter to only return results associated with the given program names. Program names are not required to be unique.

Use `GET /programs` to find the list of available programs on a server.

        Example::
            client.method
                .program_names("Better Breeding Program")
                .fetch()
                .to_df()
            
            client.method
                .program_names(["Better Breeding Program", "Best Breeding Program"])
                .fetch()
                .to_df()
            
        """
        return self._set_param("programNames", program_names)  # type: ignore[return-value]

    # --- methodDbIds ---

    def method_dbids(self, method_dbids: Union[str, List[str]]) -> "MethodQuery":
        """The unique identifier for a method.

        Example::
            client.method
                .method_dbids("3cd0ca36")
                .fetch()
                .to_df()
            
            client.method
                .method_dbids(["3cd0ca36", "983f3b14"])
                .fetch()
                .to_df()
            
        """
        return self._set_param("methodDbIds", method_dbids)  # type: ignore[return-value]

    # --- observationVariableDbIds ---

    def observation_variable_dbids(self, observation_variable_dbids: Union[str, List[str]]) -> "MethodQuery":
        """The unique identifier for an observation variable.

        Example::
            client.method
                .observation_variable_dbids("3cd0ca36")
                .fetch()
                .to_df()
            
            client.method
                .observation_variable_dbids(["3cd0ca36", "983f3b14"])
                .fetch()
                .to_df()
            
        """
        return self._set_param("observationVariableDbIds", observation_variable_dbids)  # type: ignore[return-value]

    # --- Bulk convenience ---

    def filter(
        self,
        *,
        common_crop_names: Optional[List[str]] = None,
        ontology_dbids: Optional[List[str]] = None,
        program_dbids: Optional[List[str]] = None,
        program_names: Optional[List[str]] = None,
        method_dbids: Optional[List[str]] = None,
        observation_variable_dbids: Optional[List[str]] = None,
    ) -> "MethodQuery":
        """
        Apply multiple filters in one call.  All parameters are optional; only
        non-``None`` values are added to the request.

        Example::

            (
                client.method
                    .filter(
                        common_crop_names=["Tomatillo"],
                        program_dbids=["8f5de35b"],
                        program_names=["Better Breeding Program"],
                   )
                    .fetch()
                    .to_df()
            )
        """
        q: MethodQuery = self
        return q

    # ------------------------------------------------------------------
    # Search / list — lazy BrapiResult builders
    # ------------------------------------------------------------------
    def list(
        self,
        *,
        page_size: Optional[int] = None,
    ) -> BrapiResult[Method]:
        """
        Execute the query by calling ``GET /methods``.

        Uses the same filter state built with the fluent methods.  Parameters
        that accept multiple values are passed as repeated query-string keys so
        the server can filter on all supplied values.

        .. note::
            Some filter parameters supported by ``search()``
            (``synonyms``, ``familyCodes``, ``binomialNames``, ``collections``)
            are **not** available on the list endpoint and will be silently
            ignored.

        Build filters with the fluent methods before calling this::

        Example::

            df = (
                client.method
                    .common_crop_names("Tomatillo")
                    .program_dbids("8f5de35b")
                    .program_names("Better Breeding Program")
                    .list()
                    .to_df()
            )

        Args:
            page_size: Override the page size for this call only.

        Returns:
            Lazy ``BrapiResult[Method]``.
        """
        _page_size = page_size if page_size is not None else self._page_size
        max_pages = self._max_pages

        # Translate search params (plural body keys) → GET query params (singular)
        get_params: Dict[str, Any] = {}
        for search_key, value in self._params.items():
            get_key = _LIST_PARAM_MAP.get(search_key, search_key)  # default: pass through
            if get_key is None:
                continue  # not supported on GET /true
            # Lists become repeated query-string values; scalars pass through
            get_params[get_key] = value

        http = self._http
        model_cls = self._model_cls
        to_df_fn = self._to_df_fn

        def _fetcher() -> List[Method]:
            records = http.fetch_all_pages(
                endpoint=_CRUD_ENDPOINT,
                method="GET",
                params=get_params,
                page_size=_page_size,
                max_pages=max_pages,
            )
            return [model_cls(**r) for r in records]  # type: ignore[call-arg]

        return BrapiResult(fetcher=_fetcher, to_df_fn=to_df_fn)  # type: ignore[arg-type]

    # ------------------------------------------------------------------
    # CRUD operations — execute immediately (not lazy BrapiResult)
    # ------------------------------------------------------------------
    def get_by_id(self, method_dbid: str) -> "Method":
        """
        Retrieve a single Method by its database ID.

        Calls ``GET /methods/{methodDbId}``.

        Args:
            method_dbid: The ``methodDbId`` to retrieve.

        Returns:
            A single ``Method`` object.
        """
        record = self._http.get_one(f"{_CRUD_ENDPOINT}/{method_dbid}")
        return Method(**record)
    def create(
        self,
        method: Union["Method", Dict[str, Any]],
    ) -> "Method":
        """
        Create a single Method record using ``POST //methods``.

        The server assigns ``methodDbId``; you may omit it from the input.

        Args:
            method: A ``Method`` instance or a plain dict matching the
                ``Method`` schema.

        Returns:
            The created ``Method`` object with the server-assigned
            ``methodDbId``.
        """
        body = (
            method.model_dump(mode="json", exclude_none=True)
            if isinstance(method, Method)
            else method
        )
        record = self._http.post_one(_CRUD_ENDPOINT, body)
        return Method(**record)

    def update(
        self,
        method_dbid: str,
        method: Union["Method", Dict[str, Any]],
    ) -> "Method":
        """
        Update a Method record using ``PUT //methods/{methodDbId}``.

        Args:
            method_dbid: The ``methodDbId`` of the record to update.
            Method: A ``Method`` instance or plain dict with updated fields.

        Returns:
            The updated ``Method`` object as returned by the server.
        """
        body = (
            method.model_dump(mode="json", exclude_none=True)
            if isinstance(method, Method)
            else method
        )
        record = self._http.put_one(f"{_CRUD_ENDPOINT}/{method_dbid}", body)
        return Method(**record)


# Generated by Schema Tools Generator Version: '0.57.0'