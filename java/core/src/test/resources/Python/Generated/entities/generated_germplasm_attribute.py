# GermplasmAttribute
"""
entities/generated_germplasm_attribute.py.py — GermplasmAttribute Pydantic model + GermplasmAttributeQuery.

This file is generated by the Thymeleaf Python template (EntityClass.txt).
Keep hand-written infrastructure dependencies minimal:
  - Pydantic BaseModel for the entity
  - BaseQuery from brapi._query

Generated by:
  BrAPI Schema Tools 0.57.0 (
"""
from __future__ import annotations

from typing import Any, Dict, List, Optional, Union
from datetime import date, datetime

import pandas as pd
from pydantic import BaseModel, ConfigDict

from brapi.entities.generated_trait import Trait
from brapi.entities.generated_scale import Scale
from brapi.entities.generated_method import Method
from brapi.entities.generated_germplasm_attribute_value import GermplasmAttributeValue

from brapi.generated_common import OntologyReference, ExternalReference, AdditionalInfo, TraitDataType

from .._query import BaseQuery
from .._http import HttpTransport
from .._result import BrapiResult


# ---------------------------------------------------------------------------
# Primary model
# ---------------------------------------------------------------------------

class GermplasmAttribute(BaseModel):
    """
    The Trait-Method-Scale definition for a variable, specifically variables related to Germplasm. Similar to an ObservationVariable, but related to a Germplasm instead of an ObservationUnit

    Required fields: ``attributeName``

    All other fields are optional.  Nested relationship lists (``contextOfUse``,
    ``externalReferences``,
    ``synonyms``,
    ``attributeValues``) are parsed into sub-models when present; unknown extra fields are
    accepted (``extra="allow"``) to survive schema evolution without breaking.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---
    attributeName: str
    # --- Scalar optional ---
    commonCropName: Optional[str] = None
    defaultValue: Optional[str] = None
    documentationURL: Optional[str] = None
    growthStage: Optional[str] = None
    institution: Optional[str] = None
    language: Optional[str] = None
    scientist: Optional[str] = None
    status: Optional[str] = None
    submissionTimestamp: Optional[datetime] = None
    attributeCategory: Optional[str] = None
    attributeDbId: Optional[str] = None
    attributePUI: Optional[str] = None
    attributeDescription: Optional[str] = None

    # --- Nested lists ---
    contextOfUse: Optional[List[str]] = None
    externalReferences: Optional[List[ExternalReference]] = None
    synonyms: Optional[List[str]] = None
    attributeValues: Optional[List[GermplasmAttributeValue]] = None
    # --- Relationship IDs (many-to-one; full objects are dropped in list endpoints) ---
    # These arrive as simple dicts when present; use Any to avoid failures.

    additionalInfo: Optional[Any] = None
    method: Optional[Any] = None
    ontologyReference: Optional[Any] = None
    scale: Optional[Any] = None
    trait: Optional[Any] = None


# ---------------------------------------------------------------------------
# Custom DataFrame flattener
# ---------------------------------------------------------------------------

def germplasm_attribute_to_df(items: List[GermplasmAttribute]) -> pd.DataFrame:
    """
    Convert a list of ``GermplasmAttribute`` objects to a flat ``pd.DataFrame``.

    Nested sub-model lists (``) are serialised to JSON strings so each GermplasmAttribute remains
    one row.  Relationship objects (``) are expanded one level: any ``*DbId`` and ``*Name`` fields are hoisted
    to top-level columns.
    """
    rows: List[Dict[str, Any]] = []
    for item in items:
        row = item.model_dump(mode="python", exclude_none=True)

        # Flatten one-to-one relationships: extract DbId/Name into top-level cols
        for rel in (

        ):
            obj = row.pop(rel, None)
            if isinstance(obj, dict):
                for k, v in obj.items():
                    if k.endswith(("DbId", "Name", "PUI")):
                        row[f"{rel}_{k}"] = v

        # Serialise one-to-many lists to strings (one row per GermplasmAttribute)
        for arr_field in (

        ):
            arr = row.pop(arr_field, None)
            if arr:
                import json
                row[arr_field] = json.dumps(arr, default=str)

        rows.append(row)

    return pd.DataFrame(rows)

# ---------------------------------------------------------------------------
# Query builder
# ---------------------------------------------------------------------------

_CRUD_ENDPOINT = "/attributes"
_SEARCH_ENDPOINT = "/search/attributes"

# Maps POST /search/attributes body parameter names → GET /attributes query parameter
# names.  Parameters absent from this dict (or mapped to None) are not forwarded
# to the list endpoint because GET /attributes does not support them.
_LIST_PARAM_MAP: Dict[str, Optional[str]] = {
    "germplasmDbIds": "germplasmDbId", 
    "commonCropNames": "commonCropName", 
    "programDbIds": "programDbId", 
    "methodDbIds": "methodDbId", 
    "methodNames": "methodName", 
    "methodPUIs": "methodPUI", 
    "scaleDbIds": "scaleDbId", 
    "scaleNames": "scaleName", 
    "scalePUIs": "scalePUI", 
    "traitDbIds": "traitDbId", 
    "traitNames": "traitName", 
    "traitPUIs": "traitPUI", 
    "attributeDbIds": "attributeDbId", 
    "attributeNames": "attributeName", 
    "attributePUIs": "attributePUI", 
    "attributeCategories": "attributeCategory"
    # Not supported by GET /germplasm — intentionally omitted:
    # "germplasmNames", "programNames", "studyDbIds", "studyNames", "trialDbIds", "trialNames", "studyDbId", "ontologyDbIds", "dataTypes", "traitClasses", "traitAttributes", "traitAttributePUIs", "traitEntities", "traitEntityPUIs"
}
class GermplasmAttributeQuery(BaseQuery[GermplasmAttribute]):
    """
    Fluent query builder for the BrAPI GermplasmAttribute resource.

    Create via ``BrapiClient.germplasm_attribute`` — do not instantiate directly.
    
    All filter methods return a new ``GermplasmAttributeQuery`` (immutable builder
    pattern) so the same base query can be forked::

        base = client.germplasm_attribute.germplasm_db_ids("e9c6edd7")
        q1   = base.germplasm_names("A0000003")
        q2   = base.common_crop_names("Tomatillo")
    You can provide single values one at time or a list to each filter method, for example::
        q3 = client.germplasm_attribute.germplasm_db_ids("e9c6edd7").germplasm_db_ids("1b1df4a6")  # one value as time
        q4 = client.germplasm_attribute.germplasm_db_ids(["e9c6edd7", "1b1df4a6", ...])  # list
        
    Materialise with::

        result = q1.fetch()          # BrapiResultGermplasmAttribute] (lazy)
        df     = q1.fetch().to_df()  # triggers HTTP, returns DataFrame
        items  = list(q1.fetch())    # same
    """

    def __init__(self, http: HttpTransport) -> None:
        super().__init__(
            http=http,
            endpoint=_CRUD_ENDPOINT,
            model_cls=GermplasmAttribute,
            http_method="POST",
            to_df_fn=germplasm_attribute_to_df,
        )

    # ------------------------------------------------------------------
    # Filter methods — map directly to GermplasmAttributeRequest parameters
    # ------------------------------------------------------------------


    # --- germplasmDbIds ---

    def germplasm_db_ids(self, germplasm_db_ids: Union[str, List[str]]) -> "GermplasmAttributeQuery":
        """List of IDs which uniquely identify germplasm to search for

        Example::
            client.germplasm_attribute
                .germplasm_db_ids("e9c6edd7")
                .fetch()
                .to_df()
            
            client.germplasm_attribute
                .germplasm_db_ids(["e9c6edd7", "1b1df4a6"])
                .fetch()
                .to_df()
            
        """
        return self._set_param("germplasmDbIds", germplasm_db_ids)  # type: ignore[return-value]

    # --- germplasmNames ---

    def germplasm_names(self, germplasm_names: Union[str, List[str]]) -> "GermplasmAttributeQuery":
        """List of human readable names to identify germplasm to search for

        Example::
            client.germplasm_attribute
                .germplasm_names("A0000003")
                .fetch()
                .to_df()
            
            client.germplasm_attribute
                .germplasm_names(["A0000003", "A0000477"])
                .fetch()
                .to_df()
            
        """
        return self._set_param("germplasmNames", germplasm_names)  # type: ignore[return-value]

    # --- commonCropNames ---

    def common_crop_names(self, common_crop_names: Union[str, List[str]]) -> "GermplasmAttributeQuery":
        """The BrAPI Common Crop Name is the simple, generalized, widely accepted name of the organism being researched. It is most often used in multi-crop systems where digital resources need to be divided at a high level. Things like &#39;Maize&#39;, &#39;Wheat&#39;, and &#39;Rice&#39; are examples of common crop names.

Use this parameter to only return results associated with the given crops. 

Use `GET /commoncropnames` to find the list of available crops on a server.

        Example::
            client.germplasm_attribute
                .common_crop_names("Tomatillo")
                .fetch()
                .to_df()
            
            client.germplasm_attribute
                .common_crop_names(["Tomatillo", "Paw Paw"])
                .fetch()
                .to_df()
            
        """
        return self._set_param("commonCropNames", common_crop_names)  # type: ignore[return-value]

    # --- programDbIds ---

    def program_db_ids(self, program_db_ids: Union[str, List[str]]) -> "GermplasmAttributeQuery":
        """A BrAPI Program represents the high level organization or group who is responsible for conducting trials and studies. Things like Breeding Programs and Funded Projects are considered BrAPI Programs. 

Use this parameter to only return results associated with the given programs. 

Use `GET /programs` to find the list of available programs on a server.

        Example::
            client.germplasm_attribute
                .program_db_ids("8f5de35b")
                .fetch()
                .to_df()
            
            client.germplasm_attribute
                .program_db_ids(["8f5de35b", "0e2d4a13"])
                .fetch()
                .to_df()
            
        """
        return self._set_param("programDbIds", program_db_ids)  # type: ignore[return-value]

    # --- programNames ---

    def program_names(self, program_names: Union[str, List[str]]) -> "GermplasmAttributeQuery":
        """Use this parameter to only return results associated with the given program names. Program names are not required to be unique.

Use `GET /programs` to find the list of available programs on a server.

        Example::
            client.germplasm_attribute
                .program_names("Better Breeding Program")
                .fetch()
                .to_df()
            
            client.germplasm_attribute
                .program_names(["Better Breeding Program", "Best Breeding Program"])
                .fetch()
                .to_df()
            
        """
        return self._set_param("programNames", program_names)  # type: ignore[return-value]

    # --- studyDbIds ---

    def study_db_ids(self, study_db_ids: Union[str, List[str]]) -> "GermplasmAttributeQuery":
        """List of study identifiers to search for

        Example::
            client.germplasm_attribute
                .study_db_ids("cf6c4bd4")
                .fetch()
                .to_df()
            
            client.germplasm_attribute
                .study_db_ids(["cf6c4bd4", "691e69d6"])
                .fetch()
                .to_df()
            
        """
        return self._set_param("studyDbIds", study_db_ids)  # type: ignore[return-value]

    # --- studyNames ---

    def study_names(self, study_names: Union[str, List[str]]) -> "GermplasmAttributeQuery":
        """List of study names to filter search results

        Example::
            client.germplasm_attribute
                .study_names("The First Bob Study 2017")
                .fetch()
                .to_df()
            
            client.germplasm_attribute
                .study_names(["The First Bob Study 2017", "Wheat Yield Trial 246"])
                .fetch()
                .to_df()
            
        """
        return self._set_param("studyNames", study_names)  # type: ignore[return-value]

    # --- trialDbIds ---

    def trial_db_ids(self, trial_db_ids: Union[str, List[str]]) -> "GermplasmAttributeQuery":
        """The ID which uniquely identifies a trial to search for

        Example::
            client.germplasm_attribute
                .trial_db_ids("d2593dc2")
                .fetch()
                .to_df()
            
            client.germplasm_attribute
                .trial_db_ids(["d2593dc2", "9431a731"])
                .fetch()
                .to_df()
            
        """
        return self._set_param("trialDbIds", trial_db_ids)  # type: ignore[return-value]

    # --- trialNames ---

    def trial_names(self, trial_names: Union[str, List[str]]) -> "GermplasmAttributeQuery":
        """The human readable name of a trial to search for

        Example::
            client.germplasm_attribute
                .trial_names("All Yield Trials 2016")
                .fetch()
                .to_df()
            
            client.germplasm_attribute
                .trial_names(["All Yield Trials 2016", "Disease Resistance Study Comparison Group"])
                .fetch()
                .to_df()
            
        """
        return self._set_param("trialNames", trial_names)  # type: ignore[return-value]

    # --- studyDbId ---

    def study_db_id(self, study_db_id: Union[str, List[str]]) -> "GermplasmAttributeQuery":
        """**Deprecated in v2.1** Please use `studyDbIds`. Github issue number #483 
&lt;br&gt;The unique ID of a studies to filter on

        Example::
            client.germplasm_attribute
                .study_db_id("5bcac0ae")
                .fetch()
                .to_df()
            
            client.germplasm_attribute
                .study_db_id(["5bcac0ae", "7f48e22d"])
                .fetch()
                .to_df()
            
        """
        return self._set_param("studyDbId", study_db_id)  # type: ignore[return-value]

    # --- ontologyDbIds ---

    def ontology_db_ids(self, ontology_db_ids: Union[str, List[str]]) -> "GermplasmAttributeQuery":
        """List of ontology IDs to search for

        Example::
            client.germplasm_attribute
                .ontology_db_ids("f44f7b23")
                .fetch()
                .to_df()
            
            client.germplasm_attribute
                .ontology_db_ids(["f44f7b23", "a26b576e"])
                .fetch()
                .to_df()
            
        """
        return self._set_param("ontologyDbIds", ontology_db_ids)  # type: ignore[return-value]

    # --- methodDbIds ---

    def method_db_ids(self, method_db_ids: Union[str, List[str]]) -> "GermplasmAttributeQuery":
        """List of methods to filter search results

        Example::
            client.germplasm_attribute
                .method_db_ids("07e34f83")
                .fetch()
                .to_df()
            
            client.germplasm_attribute
                .method_db_ids(["07e34f83", "d3d5517a"])
                .fetch()
                .to_df()
            
        """
        return self._set_param("methodDbIds", method_db_ids)  # type: ignore[return-value]

    # --- methodNames ---

    def method_names(self, method_names: Union[str, List[str]]) -> "GermplasmAttributeQuery":
        """Human readable name for the method
&lt;br/&gt;MIAPPE V1.1 (DM-88) Method  Name of the method of observation

        Example::
            client.germplasm_attribute
                .method_names("Measuring Tape")
                .fetch()
                .to_df()
            
            client.germplasm_attribute
                .method_names(["Measuring Tape", "Spectrometer"])
                .fetch()
                .to_df()
            
        """
        return self._set_param("methodNames", method_names)  # type: ignore[return-value]

    # --- methodPUIs ---

    def method_pu_is(self, method_pu_is: Union[str, List[str]]) -> "GermplasmAttributeQuery":
        """The Permanent Unique Identifier of a Method, usually in the form of a URI

        Example::
            client.germplasm_attribute
                .method_pu_is("http://my-traits.com/trait/CO_123:0000212")
                .fetch()
                .to_df()
            
            client.germplasm_attribute
                .method_pu_is(["http://my-traits.com/trait/CO_123:0000212", "http://my-traits.com/trait/CO_123:0003557"])
                .fetch()
                .to_df()
            
        """
        return self._set_param("methodPUIs", method_pu_is)  # type: ignore[return-value]

    # --- scaleDbIds ---

    def scale_db_ids(self, scale_db_ids: Union[str, List[str]]) -> "GermplasmAttributeQuery":
        """The unique identifier for a Scale

        Example::
            client.germplasm_attribute
                .scale_db_ids("a13ecffa")
                .fetch()
                .to_df()
            
            client.germplasm_attribute
                .scale_db_ids(["a13ecffa", "7e1afe4f"])
                .fetch()
                .to_df()
            
        """
        return self._set_param("scaleDbIds", scale_db_ids)  # type: ignore[return-value]

    # --- scaleNames ---

    def scale_names(self, scale_names: Union[str, List[str]]) -> "GermplasmAttributeQuery":
        """Name of the scale
&lt;br/&gt;MIAPPE V1.1 (DM-92) Scale Name of the scale associated with the variable

        Example::
            client.germplasm_attribute
                .scale_names("Meters")
                .fetch()
                .to_df()
            
            client.germplasm_attribute
                .scale_names(["Meters", "Liters"])
                .fetch()
                .to_df()
            
        """
        return self._set_param("scaleNames", scale_names)  # type: ignore[return-value]

    # --- scalePUIs ---

    def scale_pu_is(self, scale_pu_is: Union[str, List[str]]) -> "GermplasmAttributeQuery":
        """The Permanent Unique Identifier of a Scale, usually in the form of a URI

        Example::
            client.germplasm_attribute
                .scale_pu_is("http://my-traits.com/trait/CO_123:0000336")
                .fetch()
                .to_df()
            
            client.germplasm_attribute
                .scale_pu_is(["http://my-traits.com/trait/CO_123:0000336", "http://my-traits.com/trait/CO_123:0000560"])
                .fetch()
                .to_df()
            
        """
        return self._set_param("scalePUIs", scale_pu_is)  # type: ignore[return-value]

    # --- dataTypes ---

    def data_types(self, data_types: Union[TraitDataType, List[TraitDataType]]) -> "GermplasmAttributeQuery":
        """List of scale data types to filter search results

        Example::
            client.germplasm_attribute
                .data_types(Numerical)
                .fetch()
                .to_df()
            
            client.germplasm_attribute
                .data_types([Numerical, Ordinal])
                .fetch()
                .to_df()
            
        """
        return self._set_param("dataTypes", data_types)  # type: ignore[return-value]

    # --- traitClasses ---

    def trait_classes(self, trait_classes: Union[str, List[str]]) -> "GermplasmAttributeQuery":
        """List of trait classes to filter search results

        Example::
            client.germplasm_attribute
                .trait_classes("morphological")
                .fetch()
                .to_df()
            
            client.germplasm_attribute
                .trait_classes(["morphological", "phenological"])
                .fetch()
                .to_df()
            
        """
        return self._set_param("traitClasses", trait_classes)  # type: ignore[return-value]

    # --- traitDbIds ---

    def trait_db_ids(self, trait_db_ids: Union[str, List[str]]) -> "GermplasmAttributeQuery":
        """The unique identifier for a Trait

        Example::
            client.germplasm_attribute
                .trait_db_ids("ef81147b")
                .fetch()
                .to_df()
            
            client.germplasm_attribute
                .trait_db_ids(["ef81147b", "78d82fad"])
                .fetch()
                .to_df()
            
        """
        return self._set_param("traitDbIds", trait_db_ids)  # type: ignore[return-value]

    # --- traitNames ---

    def trait_names(self, trait_names: Union[str, List[str]]) -> "GermplasmAttributeQuery":
        """The human readable name of a trait
&lt;br/&gt;MIAPPE V1.1 (DM-86) Trait - Name of the (plant or environmental) trait under observation

        Example::
            client.germplasm_attribute
                .trait_names("Stalk Height")
                .fetch()
                .to_df()
            
            client.germplasm_attribute
                .trait_names(["Stalk Height", "Root Color"])
                .fetch()
                .to_df()
            
        """
        return self._set_param("traitNames", trait_names)  # type: ignore[return-value]

    # --- traitPUIs ---

    def trait_pu_is(self, trait_pu_is: Union[str, List[str]]) -> "GermplasmAttributeQuery":
        """The Permanent Unique Identifier of a Trait, usually in the form of a URI

        Example::
            client.germplasm_attribute
                .trait_pu_is("http://my-traits.com/trait/CO_123:0000456")
                .fetch()
                .to_df()
            
            client.germplasm_attribute
                .trait_pu_is(["http://my-traits.com/trait/CO_123:0000456", "http://my-traits.com/trait/CO_123:0000820"])
                .fetch()
                .to_df()
            
        """
        return self._set_param("traitPUIs", trait_pu_is)  # type: ignore[return-value]

    # --- traitAttributes ---

    def trait_attributes(self, trait_attributes: Union[str, List[str]]) -> "GermplasmAttributeQuery":
        """A trait can be decomposed as &quot;Trait&quot; = &quot;Entity&quot; + &quot;Attribute&quot;, the attribute is the observed feature (or characteristic) of the entity e.g., for &quot;grain colour&quot;, attribute = &quot;colour&quot;

        Example::
            client.germplasm_attribute
                .trait_attributes("Height")
                .fetch()
                .to_df()
            
            client.germplasm_attribute
                .trait_attributes(["Height", "Color"])
                .fetch()
                .to_df()
            
        """
        return self._set_param("traitAttributes", trait_attributes)  # type: ignore[return-value]

    # --- traitAttributePUIs ---

    def trait_attribute_pu_is(self, trait_attribute_pu_is: Union[str, List[str]]) -> "GermplasmAttributeQuery":
        """The Permanent Unique Identifier of a Trait Attribute, usually in the form of a URI
&lt;br/&gt;A trait can be decomposed as &quot;Trait&quot; = &quot;Entity&quot; + &quot;Attribute&quot;, the attribute is the observed feature (or characteristic) of the entity e.g., for &quot;grain colour&quot;, attribute = &quot;colour&quot;

        Example::
            client.germplasm_attribute
                .trait_attribute_pu_is("http://my-traits.com/trait/CO_123:0008336")
                .fetch()
                .to_df()
            
            client.germplasm_attribute
                .trait_attribute_pu_is(["http://my-traits.com/trait/CO_123:0008336", "http://my-traits.com/trait/CO_123:0001092"])
                .fetch()
                .to_df()
            
        """
        return self._set_param("traitAttributePUIs", trait_attribute_pu_is)  # type: ignore[return-value]

    # --- traitEntities ---

    def trait_entities(self, trait_entities: Union[str, List[str]]) -> "GermplasmAttributeQuery":
        """A trait can be decomposed as &quot;Trait&quot; = &quot;Entity&quot; + &quot;Attribute&quot;, the entity is the part of the plant that the trait refers to e.g., for &quot;grain colour&quot;, entity = &quot;grain&quot;

        Example::
            client.germplasm_attribute
                .trait_entities("Stalk")
                .fetch()
                .to_df()
            
            client.germplasm_attribute
                .trait_entities(["Stalk", "Root"])
                .fetch()
                .to_df()
            
        """
        return self._set_param("traitEntities", trait_entities)  # type: ignore[return-value]

    # --- traitEntityPUIs ---

    def trait_entity_pu_is(self, trait_entity_pu_is: Union[str, List[str]]) -> "GermplasmAttributeQuery":
        """The Permanent Unique Identifier of a Trait Entity, usually in the form of a URI
&lt;br/&gt;A trait can be decomposed as &quot;Trait&quot; = &quot;Entity&quot; + &quot;Attribute&quot;, the entity is the part of the plant that the trait refers to e.g., for &quot;grain colour&quot;, entity = &quot;grain&quot; 

        Example::
            client.germplasm_attribute
                .trait_entity_pu_is("http://my-traits.com/trait/CO_123:0004098")
                .fetch()
                .to_df()
            
            client.germplasm_attribute
                .trait_entity_pu_is(["http://my-traits.com/trait/CO_123:0004098", "http://my-traits.com/trait/CO_123:0002366"])
                .fetch()
                .to_df()
            
        """
        return self._set_param("traitEntityPUIs", trait_entity_pu_is)  # type: ignore[return-value]

    # --- attributeDbIds ---

    def attribute_db_ids(self, attribute_db_ids: Union[str, List[str]]) -> "GermplasmAttributeQuery":
        """List of Germplasm Attribute IDs to search for

        Example::
            client.germplasm_attribute
                .attribute_db_ids("2ef15c9f")
                .fetch()
                .to_df()
            
            client.germplasm_attribute
                .attribute_db_ids(["2ef15c9f", "318e7f7d"])
                .fetch()
                .to_df()
            
        """
        return self._set_param("attributeDbIds", attribute_db_ids)  # type: ignore[return-value]

    # --- attributeNames ---

    def attribute_names(self, attribute_names: Union[str, List[str]]) -> "GermplasmAttributeQuery":
        """List of human readable Germplasm Attribute names to search for

        Example::
            client.germplasm_attribute
                .attribute_names("Plant Height 1")
                .fetch()
                .to_df()
            
            client.germplasm_attribute
                .attribute_names(["Plant Height 1", "Root Color"])
                .fetch()
                .to_df()
            
        """
        return self._set_param("attributeNames", attribute_names)  # type: ignore[return-value]

    # --- attributePUIs ---

    def attribute_pu_is(self, attribute_pu_is: Union[str, List[str]]) -> "GermplasmAttributeQuery":
        """The Permanent Unique Identifier of an Attribute, usually in the form of a URI

        Example::
            client.germplasm_attribute
                .attribute_pu_is("http://my-traits.com/trait/CO_123:0008012")
                .fetch()
                .to_df()
            
            client.germplasm_attribute
                .attribute_pu_is(["http://my-traits.com/trait/CO_123:0008012", "http://my-traits.com/trait/CO_123:0007261"])
                .fetch()
                .to_df()
            
        """
        return self._set_param("attributePUIs", attribute_pu_is)  # type: ignore[return-value]

    # --- attributeCategories ---

    def attribute_categories(self, attribute_categories: Union[str, List[str]]) -> "GermplasmAttributeQuery":
        """General category for the attribute. very similar to Trait class.

        Example::
            client.germplasm_attribute
                .attribute_categories("Morphological")
                .fetch()
                .to_df()
            
            client.germplasm_attribute
                .attribute_categories(["Morphological", "Physical"])
                .fetch()
                .to_df()
            
        """
        return self._set_param("attributeCategories", attribute_categories)  # type: ignore[return-value]

    # --- Bulk convenience ---

    def filter(
        self,
        *,
        germplasm_db_ids: Optional[List[str]] = None,
        germplasm_names: Optional[List[str]] = None,
        common_crop_names: Optional[List[str]] = None,
        program_db_ids: Optional[List[str]] = None,
        program_names: Optional[List[str]] = None,
        study_db_ids: Optional[List[str]] = None,
        study_names: Optional[List[str]] = None,
        trial_db_ids: Optional[List[str]] = None,
        trial_names: Optional[List[str]] = None,
        study_db_id: Optional[List[str]] = None,
        ontology_db_ids: Optional[List[str]] = None,
        method_db_ids: Optional[List[str]] = None,
        method_names: Optional[List[str]] = None,
        method_pu_is: Optional[List[str]] = None,
        scale_db_ids: Optional[List[str]] = None,
        scale_names: Optional[List[str]] = None,
        scale_pu_is: Optional[List[str]] = None,
        data_types: Optional[List[TraitDataType]] = None,
        trait_classes: Optional[List[str]] = None,
        trait_db_ids: Optional[List[str]] = None,
        trait_names: Optional[List[str]] = None,
        trait_pu_is: Optional[List[str]] = None,
        trait_attributes: Optional[List[str]] = None,
        trait_attribute_pu_is: Optional[List[str]] = None,
        trait_entities: Optional[List[str]] = None,
        trait_entity_pu_is: Optional[List[str]] = None,
        attribute_db_ids: Optional[List[str]] = None,
        attribute_names: Optional[List[str]] = None,
        attribute_pu_is: Optional[List[str]] = None,
        attribute_categories: Optional[List[str]] = None,
    ) -> "GermplasmAttributeQuery":
        """
        Apply multiple filters in one call.  All parameters are optional; only
        non-``None`` values are added to the request.

        Example::

            (
                client.germplasm_attribute
                    .filter(
                        germplasm_db_ids=["e9c6edd7"],
                        germplasm_names=["A0000003"],
                        common_crop_names=["Tomatillo"],
                   )
                    .fetch()
                    .to_df()
            )
        """
        q: GermplasmAttributeQuery = self
        return q

    # ------------------------------------------------------------------
    # Search / list — lazy BrapiResult builders
    # ------------------------------------------------------------------
    def search(
        self,
        *,
        poll_interval: float = 2.0,
        max_poll_attempts: int = 30,
    ) -> BrapiResult[GermplasmAttribute]:
        """
        Execute the query by POSTing to ``POST /search/attributes``.

        Handles both the **synchronous** (HTTP 200, immediate ``result.data``)
        and **asynchronous** (HTTP 202, ``searchResultsDbId`` + polling) BrAPI
        search patterns transparently.

        Build filters with the fluent methods before calling this::

            df = (
                client.germplasm_attribute
                    .germplasm_db_ids("e9c6edd7")
                    .germplasm_names("A0000003")
                    .common_crop_names("Tomatillo")
                    .search()
                    .to_df()
            )

        Args:
            poll_interval: Seconds between polling retries when the server
                returns 202 (default 2).
            max_poll_attempts: Maximum number of polling retries before raising
                ``TimeoutError`` (default 30, i.e. up to 60 s wait).

        Returns:
            Lazy ``BrapiResultGermplasmAttribute``.
        """
        params = dict(self._params)
        page_size = self._page_size
        max_pages = self._max_pages
        http = self._http
        model_cls = self._model_cls
        to_df_fn = self._to_df_fn

        def _fetcher() -> List[GermplasmAttribute]:
            records = http.fetch_all_search_pages(
                endpoint=_SEARCH_ENDPOINT,
                params=params,
                page_size=page_size,
                max_pages=max_pages,
                poll_interval=poll_interval,
                max_poll_attempts=max_poll_attempts,
            )
            return [model_cls(**r) for r in records]  # type: ignore[call-arg]

        return BrapiResult(fetcher=_fetcher, to_df_fn=to_df_fn)  # type: ignore[arg-type]
    def list(
        self,
        *,
        page_size: Optional[int] = None,
    ) -> BrapiResult[GermplasmAttribute]:
        """
        Execute the query by calling ``GET /attributes``.

        Uses the same filter state built with the fluent methods.  Parameters
        that accept multiple values are passed as repeated query-string keys so
        the server can filter on all supplied values.

        .. note::
            Some filter parameters supported by ``search()``
            (``synonyms``, ``familyCodes``, ``binomialNames``, ``collections``)
            are **not** available on the list endpoint and will be silently
            ignored.

        Build filters with the fluent methods before calling this::

        Example::

            df = (
                client.germplasm_attribute
                    .germplasm_db_ids("e9c6edd7")
                    .germplasm_names("A0000003")
                    .common_crop_names("Tomatillo")
                    .list()
                    .to_df()
            )

        Args:
            page_size: Override the page size for this call only.

        Returns:
            Lazy ``BrapiResult[GermplasmAttribute]``.
        """
        _page_size = page_size if page_size is not None else self._page_size
        max_pages = self._max_pages

        # Translate search params (plural body keys) → GET query params (singular)
        get_params: Dict[str, Any] = {}
        for search_key, value in self._params.items():
            get_key = _LIST_PARAM_MAP.get(search_key, search_key)  # default: pass through
            if get_key is None:
                continue  # not supported on GET /true
            # Lists become repeated query-string values; scalars pass through
            get_params[get_key] = value

        http = self._http
        model_cls = self._model_cls
        to_df_fn = self._to_df_fn

        def _fetcher() -> List[GermplasmAttribute]:
            records = http.fetch_all_pages(
                endpoint=_CRUD_ENDPOINT,
                method="GET",
                params=get_params,
                page_size=_page_size,
                max_pages=max_pages,
            )
            return [model_cls(**r) for r in records]  # type: ignore[call-arg]

        return BrapiResult(fetcher=_fetcher, to_df_fn=to_df_fn)  # type: ignore[arg-type]

    # ------------------------------------------------------------------
    # CRUD operations — execute immediately (not lazy BrapiResult)
    # ------------------------------------------------------------------
    def get_by_id(self, attribute_db_id: str) -> "GermplasmAttribute":
        """
        Retrieve a single GermplasmAttribute by its database ID.

        Calls ``GET /attributes/{attributeDbId}``.

        Args:
            attribute_db_id: The ``attributeDbId`` to retrieve.

        Returns:
            A single ``GermplasmAttribute`` object.
        """
        record = self._http.get_one(f"{_CRUD_ENDPOINT}/{attribute_db_id}")
        return GermplasmAttribute(**record)
    def create(
        self,
        germplasm_attribute: Union["GermplasmAttribute", Dict[str, Any]],
    ) -> "GermplasmAttribute":
        """
        Create a single GermplasmAttribute record using ``POST //attributes``.

        The server assigns ``attributeDbId``; you may omit it from the input.

        Args:
            germplasm_attribute: A ``GermplasmAttribute`` instance or a plain dict matching the
                ``GermplasmAttribute`` schema.

        Returns:
            The created ``GermplasmAttribute`` object with the server-assigned
            ``attributeDbId``.
        """
        body = (
            germplasm_attribute.model_dump(mode="json", exclude_none=True)
            if isinstance(germplasm_attribute, GermplasmAttribute)
            else germplasm_attribute
        )
        record = self._http.post_one(_CRUD_ENDPOINT, body)
        return GermplasmAttribute(**record)

    def update(
        self,
        attribute_db_id: str,
        germplasm_attribute: Union["GermplasmAttribute", Dict[str, Any]],
    ) -> "GermplasmAttribute":
        """
        Update a GermplasmAttribute record using ``PUT //attributes/{attributeDbId}``.

        Args:
            attribute_db_id: The ``attributeDbId`` of the record to update.
            GermplasmAttribute: A ``GermplasmAttribute`` instance or plain dict with updated fields.

        Returns:
            The updated ``GermplasmAttribute`` object as returned by the server.
        """
        body = (
            germplasm_attribute.model_dump(mode="json", exclude_none=True)
            if isinstance(germplasm_attribute, GermplasmAttribute)
            else germplasm_attribute
        )
        record = self._http.put_one(f"{_CRUD_ENDPOINT}/{attribute_db_id}", body)
        return GermplasmAttribute(**record)


# Generated by Schema Tools Generator Version: '0.57.0'
