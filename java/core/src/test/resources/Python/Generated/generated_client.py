# BrapiClient
"""
client.py — BrapiClient: generated entity entry points.

This file is generated by the Thymeleaf Python template (BrapiClient.txt).
DO NOT EDIT MANUALLY — changes will be overwritten on the next generation run.

Handwritten infrastructure lives in _base_client.py (BaseBrapiClient).
"""
from __future__ import annotations

from ._base_client import BaseBrapiClient

from .entities import ListQuery
from .entities import LocationQuery
from .entities import PersonQuery
from .entities import ProgramQuery
from .entities import SeasonQuery
from .entities import StudyQuery
from .entities import TrialQuery
from .entities import AlleleMatrixQuery
from .entities import CallQuery
from .entities import CallSetQuery
from .entities import GenomeMapQuery
from .entities import MarkerPositionQuery
from .entities import PlateQuery
from .entities import ReferenceQuery
from .entities import ReferenceSetQuery
from .entities import SampleQuery
from .entities import VariantQuery
from .entities import VariantSetQuery
from .entities import BreedingMethodQuery
from .entities import CrossQuery
from .entities import CrossingProjectQuery
from .entities import GermplasmQuery
from .entities import GermplasmAttributeQuery
from .entities import GermplasmAttributeValueQuery
from .entities import PedigreeNodeQuery
from .entities import PlannedCrossQuery
from .entities import SeedLotQuery
from .entities import EventQuery
from .entities import ImageQuery
from .entities import MethodQuery
from .entities import ObservationQuery
from .entities import ObservationUnitQuery
from .entities import ObservationVariableQuery
from .entities import OntologyQuery
from .entities import ScaleQuery
from .entities import TraitQuery


class BrapiClient(BaseBrapiClient):
    """
    Single entry-point for all BrAPI v2 entityClasses.

    Inherits connection setup, authentication, and transport lifecycle from
    :class:`BaseBrapiClient`.  Each property below returns a fresh query object
    bound to the shared HTTP transport.

    Example::

        from brapi import BrapiClient

        client = BrapiClient(
            base_url="https://brapi.example.org",
            token_endpoint="https://brapi.example.org/token",
            client_id="svc-account",
            client_secret="...",
        )


        df = client.list.fetch().to_df()

        df = client.location.fetch().to_df()

        df = client.person.fetch().to_df()
    """

    # ------------------------------------------------------------------
    # Entity entry points
    # ------------------------------------------------------------------


    @property
    def list(self) -> ListQuery:
        """
        Entry point for List entityClasses.

        Returns a fresh :class:`ListQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.list.fetch().to_df()
        """
        return ListQuery(http=self._http)

    @property
    def location(self) -> LocationQuery:
        """
        Entry point for Location entityClasses.

        Returns a fresh :class:`LocationQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.location.fetch().to_df()
        """
        return LocationQuery(http=self._http)

    @property
    def person(self) -> PersonQuery:
        """
        Entry point for Person entityClasses.

        Returns a fresh :class:`PersonQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.person.fetch().to_df()
        """
        return PersonQuery(http=self._http)

    @property
    def program(self) -> ProgramQuery:
        """
        Entry point for Program entityClasses.

        Returns a fresh :class:`ProgramQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.program.fetch().to_df()
        """
        return ProgramQuery(http=self._http)

    @property
    def season(self) -> SeasonQuery:
        """
        Entry point for Season entityClasses.

        Returns a fresh :class:`SeasonQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.season.fetch().to_df()
        """
        return SeasonQuery(http=self._http)

    @property
    def study(self) -> StudyQuery:
        """
        Entry point for Study entityClasses.

        Returns a fresh :class:`StudyQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.study.fetch().to_df()
        """
        return StudyQuery(http=self._http)

    @property
    def trial(self) -> TrialQuery:
        """
        Entry point for Trial entityClasses.

        Returns a fresh :class:`TrialQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.trial.fetch().to_df()
        """
        return TrialQuery(http=self._http)

    @property
    def allele_matrix(self) -> AlleleMatrixQuery:
        """
        Entry point for AlleleMatrix entityClasses.

        Returns a fresh :class:`AlleleMatrixQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.allele_matrix.fetch().to_df()
        """
        return AlleleMatrixQuery(http=self._http)

    @property
    def call(self) -> CallQuery:
        """
        Entry point for Call entityClasses.

        Returns a fresh :class:`CallQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.call.fetch().to_df()
        """
        return CallQuery(http=self._http)

    @property
    def call_set(self) -> CallSetQuery:
        """
        Entry point for CallSet entityClasses.

        Returns a fresh :class:`CallSetQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.call_set.fetch().to_df()
        """
        return CallSetQuery(http=self._http)

    @property
    def genome_map(self) -> GenomeMapQuery:
        """
        Entry point for GenomeMap entityClasses.

        Returns a fresh :class:`GenomeMapQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.genome_map.fetch().to_df()
        """
        return GenomeMapQuery(http=self._http)

    @property
    def marker_position(self) -> MarkerPositionQuery:
        """
        Entry point for MarkerPosition entityClasses.

        Returns a fresh :class:`MarkerPositionQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.marker_position.fetch().to_df()
        """
        return MarkerPositionQuery(http=self._http)

    @property
    def plate(self) -> PlateQuery:
        """
        Entry point for Plate entityClasses.

        Returns a fresh :class:`PlateQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.plate.fetch().to_df()
        """
        return PlateQuery(http=self._http)

    @property
    def reference(self) -> ReferenceQuery:
        """
        Entry point for Reference entityClasses.

        Returns a fresh :class:`ReferenceQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.reference.fetch().to_df()
        """
        return ReferenceQuery(http=self._http)

    @property
    def reference_set(self) -> ReferenceSetQuery:
        """
        Entry point for ReferenceSet entityClasses.

        Returns a fresh :class:`ReferenceSetQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.reference_set.fetch().to_df()
        """
        return ReferenceSetQuery(http=self._http)

    @property
    def sample(self) -> SampleQuery:
        """
        Entry point for Sample entityClasses.

        Returns a fresh :class:`SampleQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.sample.fetch().to_df()
        """
        return SampleQuery(http=self._http)

    @property
    def variant(self) -> VariantQuery:
        """
        Entry point for Variant entityClasses.

        Returns a fresh :class:`VariantQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.variant.fetch().to_df()
        """
        return VariantQuery(http=self._http)

    @property
    def variant_set(self) -> VariantSetQuery:
        """
        Entry point for VariantSet entityClasses.

        Returns a fresh :class:`VariantSetQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.variant_set.fetch().to_df()
        """
        return VariantSetQuery(http=self._http)

    @property
    def breeding_method(self) -> BreedingMethodQuery:
        """
        Entry point for BreedingMethod entityClasses.

        Returns a fresh :class:`BreedingMethodQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.breeding_method.fetch().to_df()
        """
        return BreedingMethodQuery(http=self._http)

    @property
    def cross(self) -> CrossQuery:
        """
        Entry point for Cross entityClasses.

        Returns a fresh :class:`CrossQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.cross.fetch().to_df()
        """
        return CrossQuery(http=self._http)

    @property
    def crossing_project(self) -> CrossingProjectQuery:
        """
        Entry point for CrossingProject entityClasses.

        Returns a fresh :class:`CrossingProjectQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.crossing_project.fetch().to_df()
        """
        return CrossingProjectQuery(http=self._http)

    @property
    def germplasm(self) -> GermplasmQuery:
        """
        Entry point for Germplasm entityClasses.

        Returns a fresh :class:`GermplasmQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.germplasm.fetch().to_df()
        """
        return GermplasmQuery(http=self._http)

    @property
    def germplasm_attribute(self) -> GermplasmAttributeQuery:
        """
        Entry point for GermplasmAttribute entityClasses.

        Returns a fresh :class:`GermplasmAttributeQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.germplasm_attribute.fetch().to_df()
        """
        return GermplasmAttributeQuery(http=self._http)

    @property
    def germplasm_attribute_value(self) -> GermplasmAttributeValueQuery:
        """
        Entry point for GermplasmAttributeValue entityClasses.

        Returns a fresh :class:`GermplasmAttributeValueQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.germplasm_attribute_value.fetch().to_df()
        """
        return GermplasmAttributeValueQuery(http=self._http)

    @property
    def pedigree_node(self) -> PedigreeNodeQuery:
        """
        Entry point for PedigreeNode entityClasses.

        Returns a fresh :class:`PedigreeNodeQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.pedigree_node.fetch().to_df()
        """
        return PedigreeNodeQuery(http=self._http)

    @property
    def planned_cross(self) -> PlannedCrossQuery:
        """
        Entry point for PlannedCross entityClasses.

        Returns a fresh :class:`PlannedCrossQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.planned_cross.fetch().to_df()
        """
        return PlannedCrossQuery(http=self._http)

    @property
    def seed_lot(self) -> SeedLotQuery:
        """
        Entry point for SeedLot entityClasses.

        Returns a fresh :class:`SeedLotQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.seed_lot.fetch().to_df()
        """
        return SeedLotQuery(http=self._http)

    @property
    def event(self) -> EventQuery:
        """
        Entry point for Event entityClasses.

        Returns a fresh :class:`EventQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.event.fetch().to_df()
        """
        return EventQuery(http=self._http)

    @property
    def image(self) -> ImageQuery:
        """
        Entry point for Image entityClasses.

        Returns a fresh :class:`ImageQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.image.fetch().to_df()
        """
        return ImageQuery(http=self._http)

    @property
    def method(self) -> MethodQuery:
        """
        Entry point for Method entityClasses.

        Returns a fresh :class:`MethodQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.method.fetch().to_df()
        """
        return MethodQuery(http=self._http)

    @property
    def observation(self) -> ObservationQuery:
        """
        Entry point for Observation entityClasses.

        Returns a fresh :class:`ObservationQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.observation.fetch().to_df()
        """
        return ObservationQuery(http=self._http)

    @property
    def observation_unit(self) -> ObservationUnitQuery:
        """
        Entry point for ObservationUnit entityClasses.

        Returns a fresh :class:`ObservationUnitQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.observation_unit.fetch().to_df()
        """
        return ObservationUnitQuery(http=self._http)

    @property
    def observation_variable(self) -> ObservationVariableQuery:
        """
        Entry point for ObservationVariable entityClasses.

        Returns a fresh :class:`ObservationVariableQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.observation_variable.fetch().to_df()
        """
        return ObservationVariableQuery(http=self._http)

    @property
    def ontology(self) -> OntologyQuery:
        """
        Entry point for Ontology entityClasses.

        Returns a fresh :class:`OntologyQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.ontology.fetch().to_df()
        """
        return OntologyQuery(http=self._http)

    @property
    def scale(self) -> ScaleQuery:
        """
        Entry point for Scale entityClasses.

        Returns a fresh :class:`ScaleQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.scale.fetch().to_df()
        """
        return ScaleQuery(http=self._http)

    @property
    def trait(self) -> TraitQuery:
        """
        Entry point for Trait entityClasses.

        Returns a fresh :class:`TraitQuery` bound to the shared transport.
        Filter methods are chainable and immutable — each call returns a new
        query object leaving this initial query unmodified.

        Example::

            client.trait.fetch().to_df()
        """
        return TraitQuery(http=self._http)



# Generated by Schema Tools Generator Version: '0.57.0'
