# BrapiClient
"""
entities/.py —  Pydantic model + Query.

This file is generated by the Thymeleaf Python template (CommonClass.txt).
Keep hand-written infrastructure dependencies minimal:
  - Pydantic BaseModel for the entity
  - BaseQuery from brapi._query

Generated by:
  BrAPI Schema Tools  (
"""
from __future__ import annotations

from datetime import date, datetime
from typing import Any, Dict, List, Optional

import pandas as pd
from pydantic import BaseModel, ConfigDict

from .._query import BaseQuery
from .._http import HttpTransport


# ---------------------------------------------------------------------------
# Common-models used by more than one Entity
# ---------------------------------------------------------------------------


class AcquisitionSourceCode(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---


class AdditionalInfo(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    additionalProperties: Optional[str] = None


class AlleleMatrixPagination(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    pageSize: Optional[int] = None
    page: Optional[int] = None


class Analysis(BaseModel):
    """
    

    Required fields: ``analysisDbId``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    analysisDbId: str

    # --- Scalar optional ---

    analysisName: Optional[str] = None
    created: Optional[datetime] = None
    description: Optional[str] = None
    type: Optional[str] = None
    updated: Optional[datetime] = None


class AvailableFormat(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    expandHomozygotes: Optional[bool] = None
    fileURL: Optional[str] = None
    sepPhased: Optional[str] = None
    sepUnphased: Optional[str] = None
    unknownString: Optional[str] = None


class BiologicalStatusOfAccessionCode(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---


class CollectingInfo(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    collectingDate: Optional[date] = None
    collectingMissionIdentifier: Optional[str] = None
    collectingNumber: Optional[str] = None


class CollectingSite(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    coordinateUncertainty: Optional[str] = None
    elevation: Optional[str] = None
    georeferencingMethod: Optional[str] = None
    latitudeDecimal: Optional[str] = None
    latitudeDegrees: Optional[str] = None
    locationDescription: Optional[str] = None
    longitudeDecimal: Optional[str] = None
    longitudeDegrees: Optional[str] = None
    spatialReferenceSystem: Optional[str] = None


class Contact(BaseModel):
    """
    

    Required fields: ``contactDbId``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    contactDbId: str

    # --- Scalar optional ---

    email: Optional[str] = None
    instituteName: Optional[str] = None
    name: Optional[str] = None
    orcid: Optional[str] = None
    type: Optional[str] = None


class ContentMixture(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    mixturePercentage: Optional[int] = None


class CrossAttribute(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    crossAttributeName: Optional[str] = None
    crossAttributeValue: Optional[str] = None


class CrossParent(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---


class CrossStatus(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---


class CrossType(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---


class DataFormat(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---


class DataLink(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    dataFormat: Optional[str] = None
    description: Optional[str] = None
    fileFormat: Optional[str] = None
    name: Optional[str] = None
    provenance: Optional[str] = None
    scientificType: Optional[str] = None
    url: Optional[str] = None
    version: Optional[str] = None


class DataMatrix(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    dataMatrixAbbreviation: Optional[str] = None
    dataMatrixName: Optional[str] = None


class DataType(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---


class DatasetAuthorships(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    datasetPUI: Optional[str] = None
    license: Optional[str] = None
    publicReleaseDate: Optional[str] = None
    submissionDate: Optional[str] = None


class Dimension(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---


class DocumentationLink(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    URL: Optional[str] = None


class DocumentationLinkType(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---


class Donor(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    donorAccessionNumber: Optional[str] = None
    donorInstituteCode: Optional[str] = None


class DonorInfo(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    donorAccessionPui: Optional[str] = None
    donorAccessionNumber: Optional[str] = None


class EntryType(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---


class EnvironmentParameter(BaseModel):
    """
    

    Required fields: ``description``,
    ``environmentParametersDbId``,
    ``parameterName``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    description: str
    environmentParametersDbId: str
    parameterName: str

    # --- Scalar optional ---

    parameterPUI: Optional[str] = None
    unit: Optional[str] = None
    unitPUI: Optional[str] = None
    value: Optional[str] = None
    valuePUI: Optional[str] = None


class EventDateRange(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    endDate: Optional[datetime] = None
    startDate: Optional[datetime] = None


class EventParameter(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    code: Optional[str] = None
    description: Optional[str] = None
    name: Optional[str] = None
    units: Optional[str] = None
    value: Optional[str] = None
    valueDescription: Optional[str] = None


class ExperimentalDesign(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    PUI: Optional[str] = None
    description: Optional[str] = None


class ExternalReference(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    referenceId: Optional[str] = None
    referenceSource: Optional[str] = None


class FileFormat(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---


class GenotypeMetadata(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    fieldAbbreviation: Optional[str] = None
    fieldName: Optional[str] = None
    fieldValue: Optional[str] = None


class GeoJSON(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    type: Optional[str] = None


class GeoJSONGeometry(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---


class GeoJSONSearchArea(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    type: Optional[str] = None


class GermplasmChild(BaseModel):
    """
    

    Required fields: ``childGermplasm``,
    ``parentType``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    childGermplasm: Germplasm
    parentType: ParentType

    # --- Scalar optional ---


class GermplasmMCPD(BaseModel):
    """
    

    Required fields: ``commonCropName``,
    ``germplasm``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    commonCropName: str
    germplasm: Germplasm

    # --- Scalar optional ---

    accessionNumber: Optional[str] = None
    acquisitionDate: Optional[date] = None
    ancestralData: Optional[str] = None
    countryOfOrigin: Optional[str] = None
    genus: Optional[str] = None
    instituteCode: Optional[str] = None
    remarks: Optional[str] = None
    species: Optional[str] = None
    speciesAuthority: Optional[str] = None
    subtaxon: Optional[str] = None
    subtaxonAuthority: Optional[str] = None


class GermplasmOrigin(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    coordinateUncertainty: Optional[str] = None


class GermplasmParent(BaseModel):
    """
    

    Required fields: ``parentGermplasm``,
    ``parentType``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    parentGermplasm: Germplasm
    parentType: ParentType

    # --- Scalar optional ---


class GrowthFacility(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    PUI: Optional[str] = None
    description: Optional[str] = None


class Institute(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    instituteAddress: Optional[str] = None
    instituteCode: Optional[str] = None
    instituteName: Optional[str] = None


class LastUpdate(BaseModel):
    """
    

    Required fields: ``lastUpdateDbId``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    lastUpdateDbId: str

    # --- Scalar optional ---

    timestamp: Optional[datetime] = None
    version: Optional[str] = None


class LinkageGroup(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    linkageGroupName: Optional[str] = None
    markerCount: Optional[int] = None
    maxPosition: Optional[int] = None


class ListType(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---


class MetadataField(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    fieldAbbreviation: Optional[str] = None
    fieldName: Optional[str] = None


class MlsStatus(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---


class ObservationTreatment(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    factor: Optional[str] = None
    modality: Optional[str] = None


class ObservationUnitHierarchyLevel(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    levelName: Optional[str] = None
    levelOrder: Optional[int] = None


class ObservationUnitLevel(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    levelName: Optional[str] = None
    levelOrder: Optional[int] = None
    levelCode: Optional[str] = None


class ObservationUnitLevelRelationship(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    levelName: Optional[str] = None
    levelOrder: Optional[int] = None
    levelCode: Optional[str] = None


class ObservationUnitPosition(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    positionCoordinateX: Optional[str] = None
    positionCoordinateY: Optional[str] = None


class OntologyReference(BaseModel):
    """
    

    Required fields: ``ontology``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    ontology: Ontology

    # --- Scalar optional ---

    ontologyReferenceDbId: Optional[str] = None
    version: Optional[str] = None


class OntologyTerm(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    term: Optional[str] = None
    termURI: Optional[str] = None


class Pagination(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    page: Optional[int] = None
    pageSize: Optional[int] = None
    totalCount: Optional[int] = None
    totalPages: Optional[int] = None


class ParentType(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---


class PlannedCrossStatus(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---


class PlateFormat(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---


class PollinationEvent(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    pollinationNumber: Optional[str] = None
    pollinationSuccessful: Optional[bool] = None
    pollinationTimeStamp: Optional[datetime] = None


class PositionCoordinateType(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---


class ProgramType(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---


class Publication(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    publicationPUI: Optional[str] = None
    publicationReference: Optional[str] = None


class ReferenceBases(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    nextPageToken: Optional[str] = None
    offset: Optional[int] = None
    sequence: Optional[str] = None


class SampleType(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---


class SeedLotTransaction(BaseModel):
    """
    

    Required fields: ``transactionDbId``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    transactionDbId: str

    # --- Scalar optional ---

    amount: Optional[float] = None
    transactionDescription: Optional[str] = None
    transactionTimestamp: Optional[datetime] = None
    units: Optional[str] = None


class SortBy(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---


class SortOrder(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---


class StorageType(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    description: Optional[str] = None


class StorageTypeCode(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---


class Synonym(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    synonym: Optional[str] = None
    type: Optional[str] = None


class TaxonId(BaseModel):
    """
    

    Required fields: ``taxonId``,
    ``sourceName``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    taxonId: str
    sourceName: str

    # --- Scalar optional ---


class TraitDataType(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---


class ValidValues(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    maximumValue: Optional[str] = None
    minimumValue: Optional[str] = None


class ValidValuesCategory(BaseModel):
    """
    

    Required fields: ``

    All other fields are optional.
    """

    model_config = ConfigDict(extra="allow", populate_by_name=True)

    # --- Required ---

    # --- Scalar optional ---

    label: Optional[str] = None
    value: Optional[str] = None



# Generated by Schema Tools Generator Version: '0.57.0'
